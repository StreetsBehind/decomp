node_modules/
# Run outputs — reproducible from fixtures, not source of truth. Keep the dir, ignore contents.
runs/*
!runs/.gitkeep
*.log
.DS_Store

# Study run outputs — reproducible scratch
studies/**/runs/

# Bundle Update Log

## 2026-06-28
* **New finding (`findings/obligation-supply-boundary-stateful-seam.md`, status `confirmed`)**: Logs the meta-search scope boundary on the program's central lever. Run C (the prompt-level `--inject` ladder) showed that supplying the skeleton-**declared** obligation TEXT at authorship time is a NULL for the **stateful, cross-surface** `approve→execute`/idempotency seam — all four approval cells still wall (integration i50–63 vs an all-frontier baseline holding i100), a variance-robust unanimous failure (the `lifecycle-d1` no-op control swung ≥60pp on draw variance). This **qualifies** [obligation-blindness-tier-independent](findings/obligation-blindness-tier-independent.md) (its "supply the obligations" lever reaches stateless per-surface crosscuts but not the stateful seam) — added a reciprocal scope-boundary link there. The boundary implication is the M-coh-2.5 form of [skeleton-double-dissociation](findings/skeleton-double-dissociation.md) / [frozen-skeleton-plus-retry](findings/frozen-skeleton-plus-retry.md): the seam needs the obligation as **enforcing code in the orchestration layer**. The strong-injection test (cheap WIRES a frontier-authored primitive) is pre-registered with a null-wiring ablation but **has not yet run** — flagged as the open/provisional sub-claim inside the otherwise-confirmed boundary. Indexed under findings/"The measurements". `okf lint` GREEN after edits (41 concepts).
* **Finding updated (`findings/obligation-supply-boundary-stateful-seam.md`) — the open sub-claim is now RESOLVED**: the strong-injection terminal ladder RAN (run D, `--inject-code`, 5 cells; record `studies/meta-search/LADDER-RESULTS-D.md`). **Verdict: SCOPE-SHRINK** (reliability-half). It is a *stronger* null than the prompt-inject: the cheap pool **did** wire the frontier-authored primitive (8/8 approval draws import `_obligation.mjs` + call `enforceExecute`), yet all four approval cells still wall (worst-of-K i50/75/50/69 vs baseline i100). The pre-registered **Clause-7 perturbation adjunct** is decisive: on every parity-passing wired route, neutralizing the primitive's admin/SoD/idempotency bindings flips **zero** obligation-oracle tests → the injected primitive is **causally inert**; passing routes pass by their own re-implementation, and the worst routes botch the wiring (`enforceExecute is not defined`). So the stateful seam needs frontier **authorship of the surface**, not a primitive the cheap coder wires; Rule 2(e) is exhausted and the cheap-coder lever menu is closed for approve→execute. Replaced the "Open test (provisional)" section with "The terminal measurement", flipped `resource:`/description to run D, re-cited. Reliability-half only; the cost half is unproven and made *harder* by the lever. `okf lint` GREEN (41 concepts).

## 2026-06-19
* **Tense reconciliation (prose only, no thesis/mapping/status change)**: Reconciled future/not-yet-tense prose to the realized M5 timeline (the meta-search instrument was *brought forward* and has run P0→P2c, PROVISIONAL). Edited: `index.md` orientation blockquote (workflow-search is the instrument the program uses NOW to discover/validate the frozen config, not a tool that "comes after"); `synthesis/workflow-search-is-m5.md` (added a "prediction overtaken — now realized" banner; softened "M5, not next"/"premature"/"before any search runs over it" with dated UPDATE annotations; kept the genes/chromosome/fitness mapping table intact); `synthesis/non-gameable-fitness-wedge.md` (corrected the now-false "even though it has not yet run a search"; wedge thesis unchanged); `synthesis/two-term-fitness-existence-proof.md` (the realized M5 search DID use the two-term/per-cell-veto fitness from the start; existence-proof core unchanged). Theses, mappings, and `status` frontmatter untouched. Also fixed a dangling-looking local path in `okf/OKF-STUDY-AND-ADOPTION.md` (`okf/SPEC.md` → explicit external reference to the upstream `GoogleCloudPlatform/knowledge-catalog` spec). `okf lint` GREEN after edits.
* **Tooling**: Added `okf/tools/okf.py` — a stdlib-only `lint` (conformance gate: required keys type/title/description/timestamp + relative **and** bundle-absolute link integrity) and `viz` (self-contained interactive HTML graph). Reimplemented rather than vendored because the upstream reference viewer skips bundle-absolute links (renders 0 edges on this bundle). `okf lint` is GREEN here (40 concepts, 0 problems); verified by the canonical `OKFDocument.validate()` too. See `okf/OKF-STUDY-AND-ADOPTION.md` (R1).
* **Schema (extension key)**: Added a `status` frontmatter key to every `findings/` and `synthesis/` concept — `confirmed | provisional | superseded`. This is an OKF extension key (consumer-preserved) that encodes the program's research lifecycle, and is intended as the `confirmed`-record store the meta-search knowledge-conditioning gene was deferred for. **Rubric:** `confirmed` = settled negatives/literature/operational facts + directly-measured reliability-parity/attribution results; `provisional` = the still-unproven cost-DOMINANCE money-chart (scale-gated) + interpretive syntheses; `superseded` = a plan the realized program replaced. Current: 10 confirmed, 3 provisional (`findings/size-harness-crossover`, `synthesis/non-gameable-fitness-wedge`, `synthesis/two-term-fitness-existence-proof`), 1 superseded (`synthesis/m5-experiment-plan-gepa-first` → realized M5 = the meta-search program P0–P2c). Status values are the research lead's to ratify/adjust (one-line edits).

## 2026-06-16
* **Initialization**: Created the bundle from a research pass over the automated-agentic-workflow-optimization literature (GEPA, ADAS, AFlow, AgentSquare, AgentSwift, SwarmAgentic, AutoMaAS, OpenEvolve/AlphaEvolve, CodeEvolve, SEW, EvoFlow, MASS, AgentFactory, Meta-Context-Engineering) plus three surveys and one critique.
* **Creation**: Authored the [concepts](/concepts/index.md), [methods](/methods/index.md), and [surveys](/surveys/index.md) sections from primary sources read 2026-06-16.
* **Creation**: Authored the [findings](/findings/index.md) section from this repo's artifacts (`docs/REPORT-2026-06-16.md` and its sources).
* **Creation**: Authored the [synthesis](/synthesis/index.md) section linking the field to the program's pre-registered M5 milestone.
* **Note**: Several frontier sources (IBM survey arXiv:2603.22386, AgentFactory arXiv:2603.18000, Meta-Context-Engineering arXiv:2601.21557, the TMLR-2026 self-evolving-agents survey) are at or past a January-2026 knowledge cutoff and were surfaced/verified via live web search; their exact figures should be re-checked against the source PDFs before being load-bearing.

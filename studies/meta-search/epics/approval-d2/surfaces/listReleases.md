Implement listing releases.

  export function listReleases(ctx) { ... }

- Return an array of the release records in `ctx.db.releases`.
Return ONLY the JavaScript module code. No prose, no markdown.

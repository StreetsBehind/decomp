Implement drawing credit from a meter.

  export function consume(ctx, meterId, amount, key) { ... }

- Deduct `amount` from the meter and return a result. Throw if the meter does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

Implement granting credit to a meter.

  export function topUp(ctx, meterId, amount, key) { ... }

- Add `amount` credit to the meter and return a result. Throw if the meter does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

Implement listing vouchers.

  export function listVouchers(ctx) { ... }

- Return an array of the voucher records in `ctx.db.vouchers`.
Return ONLY the JavaScript module code. No prose, no markdown.

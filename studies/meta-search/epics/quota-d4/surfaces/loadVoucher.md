Implement granting credit to a voucher.

  export function loadVoucher(ctx, voucherId, amount, key) { ... }

- Add `amount` credit to the voucher and return a result. Throw if the voucher does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

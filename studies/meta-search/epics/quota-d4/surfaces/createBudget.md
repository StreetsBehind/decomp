Implement creating a budget.

  export function createBudget(ctx, input) { ... }

- `input` is `{ name }`. Create a new budget record, add it to `ctx.db.budgets` keyed by a new unique id, and return it.
Return ONLY the JavaScript module code. No prose, no markdown.

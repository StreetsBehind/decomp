Implement reading a publicly-visible ticket.

  export function getResolved(ctx, ticketId) { ... }

- Return the ticket record. Throw if the ticket does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

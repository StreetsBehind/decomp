Implement listing articles.

  export function listArticles(ctx) { ... }

- Return an array of the article records in `ctx.db.articles`.
Return ONLY the JavaScript module code. No prose, no markdown.

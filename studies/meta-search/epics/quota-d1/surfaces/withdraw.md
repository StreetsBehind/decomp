Implement drawing credit from a wallet.

  export function withdraw(ctx, walletId, amount, key) { ... }

- Deduct `amount` from the wallet and return a result. Throw if the wallet does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

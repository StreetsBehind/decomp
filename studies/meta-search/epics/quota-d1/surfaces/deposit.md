Implement granting credit to a wallet.

  export function deposit(ctx, walletId, amount, key) { ... }

- Add `amount` credit to the wallet and return a result. Throw if the wallet does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

Implement listing requests.

  export function listRequests(ctx) { ... }

- Return an array of the request records in `ctx.db.requests`.
Return ONLY the JavaScript module code. No prose, no markdown.

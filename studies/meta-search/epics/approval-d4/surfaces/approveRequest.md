Implement approving a request.

  export function approveRequest(ctx, requestId) { ... }

- Record that the request is approved by the calling user, and return the approval. Throw if the request does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

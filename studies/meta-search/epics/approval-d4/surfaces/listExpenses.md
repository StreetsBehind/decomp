Implement listing expenses.

  export function listExpenses(ctx) { ... }

- Return an array of the expense records in `ctx.db.expenses`.
Return ONLY the JavaScript module code. No prose, no markdown.

Implement approving a expense.

  export function approveExpense(ctx, expenseId) { ... }

- Record that the expense is approved by the calling user, and return the approval. Throw if the expense does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

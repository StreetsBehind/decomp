<!-- GENERATED by gen-diverse-epics.mjs — do not hand-edit; change the template in epics-src/<topology>.mjs instead. -->

All functions share one context object `ctx`:

- `ctx.session` — the authenticated caller: `{ userId, orgId, role }`.
- `ctx.db.users` — a Map from userId to `{ id, orgId, name, bio, role }` (`role` is `'member'` or `'admin'`).
- `ctx.db.requests` — a Map from requestId to a request record `{ id, orgId, requesterId, status, body }`.
- `ctx.db.releases` — a Map from releaseId to a release record `{ id, orgId, requesterId, status, body }`.
- `ctx.db.payouts` — a Map from payoutId to a payout record `{ id, orgId, requesterId, status, body }`.
- `ctx.db.auditLog` — an array of execution-audit records `{ id, requestId, executedBy, orgId, at }`.
- `ctx.db.shipLog` — an array of execution-audit records `{ id, releaseId, executedBy, orgId, at }`.
- `ctx.db.settleLog` — an array of execution-audit records `{ id, payoutId, executedBy, orgId, at }`.

The system is multi-tenant: every record belongs to an organization (`orgId`).

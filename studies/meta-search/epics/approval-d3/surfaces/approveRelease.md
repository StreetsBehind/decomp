Implement approving a release.

  export function approveRelease(ctx, releaseId) { ... }

- Record that the release is approved by the calling user, and return the approval. Throw if the release does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

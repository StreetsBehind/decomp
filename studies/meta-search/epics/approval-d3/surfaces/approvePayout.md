Implement approving a payout.

  export function approvePayout(ctx, payoutId) { ... }

- Record that the payout is approved by the calling user, and return the approval. Throw if the payout does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

Implement reading a publicly-visible article.

  export function getLive(ctx, articleId) { ... }

- Return the article record. Throw if the article does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

Implement reading a publicly-visible order.

  export function getFulfilled(ctx, orderId) { ... }

- Return the order record. Throw if the order does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

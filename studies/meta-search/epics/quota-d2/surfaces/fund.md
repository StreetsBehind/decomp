Implement granting credit to a budget.

  export function fund(ctx, budgetId, amount, key) { ... }

- Add `amount` credit to the budget and return a result. Throw if the budget does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

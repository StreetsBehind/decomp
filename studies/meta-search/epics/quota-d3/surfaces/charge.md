Implement drawing credit from a budget.

  export function charge(ctx, budgetId, amount, key) { ... }

- Deduct `amount` from the budget and return a result. Throw if the budget does not exist.
Return ONLY the JavaScript module code. No prose, no markdown.

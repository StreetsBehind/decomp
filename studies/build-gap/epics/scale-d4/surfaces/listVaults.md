Implement listing vaults.

Write an ES module that exports a function:

  export function listVaults(ctx) { ... }

- Return an array of the vault records in `ctx.db.vaults`.

Return ONLY the JavaScript module code. No prose, no markdown.

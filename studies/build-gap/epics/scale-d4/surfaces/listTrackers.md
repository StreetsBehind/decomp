Implement listing trackers.

Write an ES module that exports a function:

  export function listTrackers(ctx) { ... }

- Return an array of the tracker records in `ctx.db.trackers`.

Return ONLY the JavaScript module code. No prose, no markdown.

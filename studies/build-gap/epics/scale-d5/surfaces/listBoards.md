Implement listing boards.

Write an ES module that exports a function:

  export function listBoards(ctx) { ... }

- Return an array of the board records in `ctx.db.boards`.

Return ONLY the JavaScript module code. No prose, no markdown.

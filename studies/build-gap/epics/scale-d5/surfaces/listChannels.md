Implement listing channels.

Write an ES module that exports a function:

  export function listChannels(ctx) { ... }

- Return an array of the channel records in `ctx.db.channels`.

Return ONLY the JavaScript module code. No prose, no markdown.
